<!-- Validation failure messages -->
<% if @meal_time.errors.any? %>
  <% @meal_time.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Meal Time #<%= @meal_time.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_meal_time/<%= @meal_time.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for meal -->
      <div class="form-group">
        <label for="meal" class="control-label">
          Meal
        </label>

        <input type="text" id="meal" name="meal" placeholder="Meal" class="form-control" value="<%= @meal_time.meal %>">
      </div>

      <button class="btn btn-warning">
        Update Meal Time
      </button>
      or
      <a href="/meal_times/<%= @meal_time.id %>">Cancel</a>
    </form>
  </div>
</div>
